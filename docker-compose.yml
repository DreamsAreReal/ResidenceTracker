version: '3.1'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ResidenceTrackerDb
    ports:
      - "5432:5432"
